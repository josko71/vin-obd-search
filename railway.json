{
  "build": {
    "installCommand": "pip install --no-cache-dir -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "gunicorn avto_vin_obd_projekt.wsgi --bind 0.0.0.0:$PORT --workers 3 --timeout 120 --preload"
  }
}
